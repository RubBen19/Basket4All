package com.example.basket4all.common.enums

/**
 * Enum class que sirve para almacenar las posiciones que puede tener un jugador
 */
enum class PlayerPositions(val number: String) {
    ESCOLTA("1"),
    BASE("2"),
    ALERO("3"),
    ALAPIVOT("4"),
    PIVOT("5")
}